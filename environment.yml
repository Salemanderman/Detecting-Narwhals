name: narwhal_env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pytorch=2.5.*
  - torchaudio=2.5.*
  - matplotlib
  - ipykernel
  - libsndfile
  - ffmpeg
  - scipy
  - pandas>=1.3
  - numpy>=1.24
  # remove sox on Windows unless you specifically need CLI sox